void Oude_programma(){
  if (results.value == 854656239 && programma == 2){ // Geraakt door team blauw
        digitalWrite(roodLED, HIGH);
    digitalWrite(groenLED, HIGH);
    digitalWrite(blauwLED, HIGH);
  digitalWrite(blauwLED, LOW);
}
  if (results.value == 2001751733 && programma == 2){ // Geraakt door team geel
        digitalWrite(roodLED, HIGH);
    digitalWrite(groenLED, HIGH);
    digitalWrite(blauwLED, HIGH);
  digitalWrite(roodLED, LOW);
  digitalWrite(groenLED, LOW);
}
  if (results.value == 543851703 && programma == 2){ // Geraakt door team rood
        digitalWrite(roodLED, HIGH);
    digitalWrite(groenLED, HIGH);
    digitalWrite(blauwLED, HIGH);
  digitalWrite(roodLED, LOW);
}
  if (results.value == 3915437507 && programma == 2){ // Geraakt door team groen
        digitalWrite(roodLED, HIGH);
    digitalWrite(groenLED, HIGH);
    digitalWrite(blauwLED, HIGH);
  digitalWrite(groenLED, LOW);
}

  }
